<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Noto+Serif+KR&family=Roboto:wght@400;500&display=swap" rel="stylesheet" />
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <title>Main</title>
    <style>
      * {
        font-family: "Noto Serif KR", serif;
        color: #78715e;
      }

      body {
        background-color: #ece8e4;
        position: relative;
      }

      .goButton:hover {
        border: 1px solid #78715e;
        color: #78715e;
        border-radius: 7px;
      }

      .goButton {
        border: 1px solid #78715e;
        background-color: whitesmoke;
        background-color: #78715e;
        color: whitesmoke;
        border-radius: 7px;
      }

      .mainbox {
        background-color: whitesmoke;
        border-radius: 17px;
      }
    </style>
  </head>
  <body>
    <div class="container col-12 d-flex align-items-center justify-content-center " style="min-height: 100vh;">
      <div class="mainbox col-lg-9 col-12 mx-auto p-4 mt-3 mb-3 " style=" text-align: center; ">

        <a href="/user/login">
          <img src="/logo.png" alt="logo" style="height: 6rem" />
        </a>

        <hr />

        <div class="mt-5 mb-5">
          <div class="text_box">
            <h3 class="text"></h3><br />
          </div>
          <!-- <h3 class="fw-semibold">blogging is happy. enjoy 🙂</h3><br /> -->
          '블로깅은 즐거워~'를 모티브로 하고 있습니다. 🙂<br />
          <p class="fw-bolder">블로깅에 대한 부담을 줄여줍니다.</p> <br />
          여러분은 자신의 이야기를 나누며<br /> 다른 사람들 과의 communication을 통해 행복할 것이고,<br />
          (n)on blogging은 당신을 도울 것입니다.<br />
        </div>

        <hr />

        <button type="button" class="btn btn-lg goButton mt-4 mb-4 w-100" onclick="onClick()">GO</button>

      </div>
    </div>
  </body>

  <script>
    const onClick = () => {
      location.href = "/user/login";
    };

    const content = "blogging is happy. enjoy :)";
    const text = document.querySelector(".text");
    let i = 0;

    function typing() {
      if (i < content.length) {
        let txt = content[i++];
        text.innerHTML += txt === "\n" ? "<br/>" : txt;
        setTimeout(typing, 200);
      } else {
        setTimeout(() => {
          text.textContent = "";
          i = 0;
          typing(); // 다시 시작하도록 호출
        }, 1000);
      }
    }

    typing(); // 초기 호출
  </script>
</html>